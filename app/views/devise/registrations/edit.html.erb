<div class="booyah-box col-10 offset-1">
  <h2>Edit <%= resource_name.to_s.humanize %></h2>

  <%= simple_form_for(resource, as: resource_name, url: user_registration_path, method: :patch) do |f| %>
    <%= devise_error_messages! %>
    
    <%= f.input :first_name %>
    <%= f.input :last_name %>
    <%= f.input :username %>
    <%= f.input :birthday, as: :date, start_year: Date.today.year - 90,
                            end_year: Date.today.year - 18, order: [:month, :day, :year] %>
    <%= f.input :about, placeholder: "Write about yourself" %>
    <%= f.input :quote, as: :text %>
    <%= f.label "What type of adventurer are you?" %>
    <%= f.select :adventurer_type, options_for_select(adventurer_type) %>
    
    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <%= f.input :password, label: "New Password (leave blank if you prefer not to change it)", placeholder: "#{@minimum_password_length} characters minimun" %>
    <%= f.input :password_confirmation, placeholder: "re-type new password" %><br />
    <%= f.input :current_password, placeholder: "We need you current password for confirmation" %>
    <%= f.submit "Update" %>
  <% end %>

  <h3>Cancel my account</h3>

  <p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "All of your information, adventures, and posts will be permanently deleted. Are you sure?" }, method: :delete, class: "btn btn-danger" %></p>

  <%= link_to "Back", :back %>
</div>
